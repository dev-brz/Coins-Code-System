server.port: 8080

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  liquibase.enabled: false
  sql.init.mode: always

user:
  profile:
    dir: "${user.home}/coin-code/test/user/profile-images"
    image-url: "http://localhost:${server.port}/users/image?name="

coin:
  image:
    dir: "${user.home}/coin-code/test/coin/images"